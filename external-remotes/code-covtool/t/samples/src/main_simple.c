#include "../include/sample_functions.h"
#include <stdio.h>

int main() {
    // Минимальное покрытие
    printf("Math: %d\n", simple_math(2, 3));
    printf("Check: %d\n", check_value(25));
    printf("Sum: %d\n", calculate_sum(3));

    return 0;
}
